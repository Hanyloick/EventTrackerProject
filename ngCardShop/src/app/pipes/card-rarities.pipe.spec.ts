import { CardRaritiesPipe } from './card-rarities.pipe';

describe('CardRaritiesPipe', () => {
  it('create an instance', () => {
    const pipe = new CardRaritiesPipe();
    expect(pipe).toBeTruthy();
  });
});
